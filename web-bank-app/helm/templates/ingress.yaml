apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: web-bank-app-ingress
  annotations:
    kubernetes.io/ingress.class: alb
    alb.ingress.kubernetes.io/scheme: internet-facing
    alb.ingress.kubernetes.io/target-type: ip
spec:
  rules:
    - host: web-bank-app.example.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: web-bank-app-service
                port:
                  number: 80
